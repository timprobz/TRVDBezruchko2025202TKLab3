<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - {{siteTitle}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body>
    <!-- Header -->
    <header class="bg-primary text-white py-3">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">📚 {{siteTitle}}</h1>
                <div class="d-flex align-items-center">
                    {{#isAuthenticated}}
                    <span class="me-3">Вітаємо, {{user.firstName}}!</span>
                    <div class="dropdown">
                        <button class="btn btn-outline-light btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            Мій акаунт
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/auth/profile"><i class="bi bi-person"></i> Мій профіль</a></li>
                            {{#isLibrarian}}
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/books/create"><i class="bi bi-book-plus"></i> Додати книгу</a></li>
                            <li><a class="dropdown-item" href="/users"><i class="bi bi-people"></i> Користувачі</a></li>
                            {{/isLibrarian}}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/auth/logout" method="POST" class="d-inline">
                                    <button type="submit" class="dropdown-item"><i class="bi bi-box-arrow-right"></i> Вийти</button>
                                </form>
                            </li>
                        </ul>
                    </div>
                    {{/isAuthenticated}}
                    {{^isAuthenticated}}
                    <a href="/auth/login" class="btn btn-outline-light btn-sm me-2">Вхід</a>
                    <a href="/auth/register" class="btn btn-light btn-sm">Реєстрація</a>
                    {{/isAuthenticated}}
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link {{#isHome}}active{{/isHome}}" href="/">Головна</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#isBooks}}active{{/isBooks}}" href="/books">Книги</a>
                    </li>
                    {{#isAuthenticated}}
                    {{#isLibrarian}}
                    <li class="nav-item">
                        <a class="nav-link {{#isUsers}}active{{/isUsers}}" href="/users">Користувачі</a>
                    </li>
                    {{/isLibrarian}}
                    {{/isAuthenticated}}
                    <li class="nav-item">
                        <a class="nav-link {{#isAbout}}active{{/isAbout}}" href="/about">Про сайт</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container my-4">
        {{#successMessage}}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{successMessage}}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/successMessage}}
        
        {{#errorMessage}}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            {{errorMessage}}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/errorMessage}}
        
        {{{content}}}
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white py-4 mt-4">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>{{siteTitle}}</h5>
                    <p>Електронна бібліотека для зберігання та управління українською літературою</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p>© {{currentYear}} Всі права захищені</p>
                    <p>Контакти: info@ukrlibrary.com</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>